import { Container, Flex } from "@mantine/core";

function Persona() {
  return (
    <Container bg="teal.6">
      <Flex h="300px" w="full">
        Sobre Min
      </Flex>
    </Container>
  );
}

export default Persona;
