import { Container, Flex } from "@mantine/core";

function Navbar() {
  return (
    <Container bg="teal.1">
      <Flex h="300px" w="full">
        Navbar
      </Flex>
    </Container>
  );
}

export default Navbar;
